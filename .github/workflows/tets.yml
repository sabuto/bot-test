name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      name: Main Checkout

    # - uses: actions/checkout@v2
    #   name: Secondary Checkout
    #   with:
    #     repository: sabuto/bot-test2
    #     path: bot-test2
    - name: do ls etc
      run: |
        pwd
        ls
        cd ../
        ls
